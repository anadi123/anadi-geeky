#include<bits/stdc++.h>
using namespace std;
int main()
{
	string str;int maxc=1,count,n;
	cin>>str;
	n=str.length();
	for(int i=0;i<n;i++)
	{	
		count=1;
		for(int j=i+1;str[j]==str[i];j++)
		{
			count++;
		}
		if(maxc<count)
			maxc=count;
	//	cout<<maxc;	
	}
	cout<<maxc;
	return 0;
}
